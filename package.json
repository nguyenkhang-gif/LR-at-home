{
  "name": "lightroomathome",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "cd wasm-src && wasm-pack build --target web --out-dir ../public/wasm",
    "dev": "npm run build:wasm && npx serve . -p 3030",
    "build": "npm run build:wasm"
  }
}
